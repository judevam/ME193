legoeducation==1.1.1      # lelib.py, main.py — LEGO Education hub BLE protocol
mediapipe                 # arm_control_car.py — pose landmark tracking (not part of the course's shared "useful libraries" bundle)
opencv-contrib-python==5.0.0.93  # camlib.py — camera enumeration/capture, plus cv2.aruco for
                                  # the aprilTags/ scripts (NOT the -headless variant). The
                                  # course pins plain opencv-python==4.13.0.92, but mediapipe
                                  # transitively requires opencv-contrib-python regardless -
                                  # installing both together corrupts the shared cv2 package
                                  # (confirmed: cv2.__version__ breaks), so only contrib is
                                  # installed here. It's a strict superset of opencv-python.
numpy>=1.24               # camlib.py — frame arrays
sounddevice>=0.4          # miclib.py — audio device listing
paho-mqtt>=2.0            # mqttlib.py — MQTT publish/subscribe
